# {{ ansible_managed }}
server {
    listen 80 default_server;
    root /var/www/html;
    index index.html;

    location / {
        auth_basic "Lab";
        auth_basic_user_file /etc/nginx/.htpasswd;
    }
}
